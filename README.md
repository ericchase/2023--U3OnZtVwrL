### Svelte Project

```sh
# create a new project in the current directory
pnpm create svelte@latest
# install packages
pnpm i
# start dev server
pnpm dev
```

### Modified Files

`src/app.html`  
`.prettierrc`
